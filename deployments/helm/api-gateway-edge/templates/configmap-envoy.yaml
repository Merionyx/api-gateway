apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agwedge.fullname" . }}-envoy
  labels:
    {{- include "agwedge.labels" . | nindent 4 }}
data:
  envoy.xds-tls.yaml: |
{{ include "agwedge.envoy.bootstrapYaml" . | nindent 4 }}
