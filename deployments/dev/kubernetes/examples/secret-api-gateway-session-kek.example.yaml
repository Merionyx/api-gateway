# Example only — DO NOT commit real values. Apply after replacing the placeholder.
#   openssl rand -base64 32
# Then:
#   kubectl apply -n api-gateway -f secret-api-gateway-session-kek.example.yaml
#
# Or create imperatively:
#   kubectl create secret generic api-gateway-session-kek -n api-gateway \
#     --from-literal=kek="$(openssl rand -base64 32)" --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-session-kek
  namespace: api-gateway
type: Opaque
stringData:
  kek: "<placeholder>"
