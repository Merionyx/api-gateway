package usecase

// RegistryPayloadVersionV1 is the controller→API-Server register/heartbeat DTO version with extended
// meta (provenance layer_detail, materialized observability, service scope, K8s refs).
const RegistryPayloadVersionV1 = int32(1)
