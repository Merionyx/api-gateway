// Package oidc provides a small generic OIDC client: discovery document, authorization-code token exchange,
// and RS256 id_token validation against the provider JWKS.
//
// HTTP handlers that expose login/callback are out of scope here.
package oidc
