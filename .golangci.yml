version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - ineffassign
    - misspell
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - staticcheck
