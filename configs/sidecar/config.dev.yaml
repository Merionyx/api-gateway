server:
  grpc_port: "9001"
  host: "0.0.0.0"

controller:
  address: "gateway-controller-dev-1:19090"
  environment: "dev"

jwt:
  jwks_url: "http://gateway-ha-lb:8080/.well-known/jwks-edge.json"
  # expected_issuer / expected_audience: defaults match API Server jwt.edge_* (override if you customize Edge iss/aud).

# OpenTelemetry (optional)
# telemetry:
#   enabled: false
#   otlp_endpoint: "http://jaeger:4317"
#   otlp_insecure: true
